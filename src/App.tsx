import { SplitText, LoveForm } from './components';

export default function App() {
  return (
    <main className="w-full h-screen flex items-center justify-center">
      <LoveForm
      />
    </main>
  );
}
